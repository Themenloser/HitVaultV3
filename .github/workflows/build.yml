name: Build HitVaultV3

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
      - uses: actions/setup-node@v4
        with:
          node-version: '20'  # Fix für toReversed()
          cache: 'npm'
      - run: npm ci  # ci statt install für Lockfile
      - run: |
          cd android
          ./gradlew assembleRelease
      - uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: android/app/build/outputs/apk/release/app-release.apk

  ios:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'  # Fix für Metro toReversed()
          cache: 'npm'
      - run: npm ci  # ci statt install
      - run: |
          cd ios
          pod install
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Metro Bundle Pre-Fix
        run: |
          npx react-native --version
          npx metro --version
          echo "Resetting Metro cache..."
          npx react-native start --reset-cache --verbose
      - name: Archive iOS
        run: |
          cd ios
          xcodebuild \
            -workspace HitVaultV3.xcworkspace \
            -scheme HitVaultV3 \
            -configuration Release \
            -sdk iphoneos \
            -archivePath ./build/HitVaultV3.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            clean archive
      - name: Export IPA
        run: |
          cd ios
          cat > ExportOptions.plist <<'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>development</string>
            <key>compileBitcode</key>
            <false/>
          </dict>
          </plist>
          EOF
          xcodebuild -exportArchive \
            -archivePath ./build/HitVaultV3.xcarchive \
            -exportOptionsPlist ./ExportOptions.plist \
            -exportPath ./build/export
      - uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: ios/build/export/*.ipa
